<!--index.wxml-->
<view class="container">
  <map longitude="{{longtitude}}" latitude="{{latitude}}" id="ofo-map" scale="16" controls="{{controls}}" show-location="true" bindcontroltap="bindcontroltap"></map >
</view>
